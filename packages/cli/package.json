{
  "name": "@mise/cli",
  "version": "0.1.0",
  "description": "CLI — thin commands over core",
  "type": "module",
  "bin": {
    "recipe": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@mise/core": "workspace:*",
    "commander": "^13.0.0"
  }
}
